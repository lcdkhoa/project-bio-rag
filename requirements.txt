# Biology RAG Dependencies

# Core ML/AI
torch>=2.0.0
transformers>=4.40.0
accelerate>=0.30.0
huggingface-hub>=0.23.0
sentence-transformers>=2.7.0

# LangChain
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.1.0
langchain-text-splitters>=0.2.0
# Pin a compatible langchain-chroma / chromadb pair: unbounded upper limits let a
# fresh install resolve an incompatible pair (langchain-chroma needing a chromadb
# `Search` API absent in older chromadb -> KeyError '_type' when opening the DB).
# Known-good combo used to build the current DB: langchain-chroma 0.2.6 + chromadb 1.5.9.
langchain-chroma>=0.2.6,<0.3
langchain-huggingface

# Vector Database
chromadb>=1.5.0,<2.0

# PDF & OCR
pypdf>=4.2.0
pdf2image
pytesseract
pymupdf>=1.23.0
opencv-python>=4.9.0

# Image Processing
Pillow>=10.0.0

# Utilities
python-dotenv
tqdm
rapidocr-onnxruntime

# Web Server
Flask>=3.0.0
Flask-CORS>=4.0.0

# Evaluation
langchain-openai>=0.1.0          # judge/generator qua endpoint OpenAI-compatible (MiMo/Groq/OpenRouter)
pandas>=2.0.0
rapidfuzz

# Testing
pytest
