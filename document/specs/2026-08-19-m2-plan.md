# M2 — Retrieval reranking + citation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm cross-encoder reranker (bge-reranker-v2-m3) cho cả text lẫn ảnh, chuyển embedding text sang bge-m3, và sinh citation xác định (kèm tên mục sidebar) từ metadata chunk thật — không để LLM bịa số trang.

**Architecture:** Một module reranker dùng chung (`src/rag/reranker.py`, lazy singleton, GPU/CPU-safe). Text: `RerankedRetriever` mới trong `vectorstore.py` chọn qua cờ `RERANK_ENABLED`, chảy tự động vào `HybridRetriever`. Ảnh: cộng một term cross-encoder vào `ImageVectorDB._rerank` (có cờ tắt). Citation: module thuần `src/rag/citations.py`, cắm vào `api.py::prepare_chat_payload` (đường chat production KHÔNG dùng LCEL chain — `chain.py` để nguyên).

**Tech Stack:** Python, `sentence-transformers` (CrossEncoder), PyTorch, ChromaDB/langchain-chroma, pydantic v2, pytest.

**Spec:** `document/specs/2026-08-19-m2-retrieval-rerank-citation-design.md`

## Global Constraints

- **Windows-primary dev.** Chạy test bằng `python -m pytest <path> -v` từ repo root.
- **Chỉ chạy test tập trung của phần đang sửa khi lặp** (CLAUDE.md). Chạy toàn bộ suite chỉ khi đóng milestone.
- **Giữ test nhỏ, dùng fake/mock** — KHÔNG nạp model bge-reranker/CLIP thật trong unit test (chậm/nặng). Integration nạp model thật phải `skipif` khi model/GPU vắng.
- **Commit messages: KHÔNG có `Co-Authored-By` / "Generated with"**. Plain messages.
- **Phản biện chống bug ẩn trước khi báo done** (D-06): hướng sắp xếp điểm (cross-encoder cao = liên quan cao, ngược chiều distance), ngưỡng gate, đúng tập docs cho citation, không lật exact phrase match ở ảnh.
- Config đọc từ `.env`/`src/config.py`, không hardcode. Giá trị mặc định (verbatim): `RERANK_ENABLED=true`, `RERANK_MODEL=BAAI/bge-reranker-v2-m3`, `RERANK_FETCH_K=20`, `RERANK_SCORE_MIN=0.2`, `IMAGE_RERANK_ENABLED=true`, `IMAGE_RERANK_TOP_N=12`, `IMAGE_RERANK_WEIGHT=0.25`, `EMBEDDING_MODEL` default → `BAAI/bge-m3`.
- Reranker/RerankedRetriever/ImageVectorDB nhận **reranker tiêm được** (dependency injection) để test bằng fake.

---

### Task 1: Config — rerank block + bge-m3 default

**Files:**
- Modify: `src/config.py` (đổi 1 dòng `EMBEDDING_MODEL`; thêm block M2 sau dòng ~137)
- Test: `tests/rag/test_config_m2.py`

**Interfaces:**
- Produces: hằng số config `RERANK_ENABLED: bool`, `RERANK_MODEL: str`, `RERANK_FETCH_K: int`, `RERANK_SCORE_MIN: float`, `IMAGE_RERANK_ENABLED: bool`, `IMAGE_RERANK_TOP_N: int`, `IMAGE_RERANK_WEIGHT: float`; `EMBEDDING_MODEL` default = `"BAAI/bge-m3"`.

- [ ] **Step 1: Write the failing test**

```python
# tests/rag/test_config_m2.py
import importlib


def test_m2_config_defaults(monkeypatch):
    # Clear any env overrides so we assert the coded defaults.
    for key in ["EMBEDDING_MODEL", "RERANK_ENABLED", "RERANK_MODEL",
                "RERANK_FETCH_K", "RERANK_SCORE_MIN", "IMAGE_RERANK_ENABLED",
                "IMAGE_RERANK_TOP_N", "IMAGE_RERANK_WEIGHT"]:
        monkeypatch.delenv(key, raising=False)
    import src.config as cfg
    importlib.reload(cfg)

    assert cfg.EMBEDDING_MODEL == "BAAI/bge-m3"
    assert cfg.RERANK_ENABLED is True
    assert cfg.RERANK_MODEL == "BAAI/bge-reranker-v2-m3"
    assert cfg.RERANK_FETCH_K == 20
    assert cfg.RERANK_SCORE_MIN == 0.2
    assert cfg.IMAGE_RERANK_ENABLED is True
    assert cfg.IMAGE_RERANK_TOP_N == 12
    assert cfg.IMAGE_RERANK_WEIGHT == 0.25
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/rag/test_config_m2.py -v`
Expected: FAIL (attributes not found / EMBEDDING_MODEL still MiniLM).

- [ ] **Step 3: Write minimal implementation**

Đổi dòng `EMBEDDING_MODEL` trong `src/config.py`:

```python
EMBEDDING_MODEL = os.getenv("EMBEDDING_MODEL", "BAAI/bge-m3")
```

Thêm vào cuối `src/config.py`:

```python
# --- M2: reranker + citation ---
RERANK_ENABLED = os.getenv("RERANK_ENABLED", "true").lower() == "true"
RERANK_MODEL = os.getenv("RERANK_MODEL", "BAAI/bge-reranker-v2-m3")
# Candidates pulled from the vector store before cross-encoder reranking.
RERANK_FETCH_K = int(os.getenv("RERANK_FETCH_K", "20"))
# Absolute safety gate on the rerank score (0..1): a chunk below this is
# dropped so an all-irrelevant fetch yields no context (LLM emits fallback).
RERANK_SCORE_MIN = float(os.getenv("RERANK_SCORE_MIN", "0.2"))
# Image side: cross-encoder is an additive term over the manual fusion score.
IMAGE_RERANK_ENABLED = os.getenv("IMAGE_RERANK_ENABLED", "true").lower() == "true"
IMAGE_RERANK_TOP_N = int(os.getenv("IMAGE_RERANK_TOP_N", "12"))
IMAGE_RERANK_WEIGHT = float(os.getenv("IMAGE_RERANK_WEIGHT", "0.25"))
```

Tạo `tests/rag/__init__.py` (rỗng) nếu chưa có.

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/rag/test_config_m2.py -v` → Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/config.py tests/rag/
git commit -m "feat(config): bge-m3 default + M2 rerank config block (M2 task 1)"
```

> **Adversarial note:** `importlib.reload(cfg)` re-parses defaults; các module đã import hằng số cũ sẽ không tự cập nhật — trong app thực, config đọc lúc import là đủ. Test chỉ khẳng định default, không kiểm hành vi runtime.

---

### Task 2: `CrossEncoderReranker` + singleton (dùng chung text/ảnh)

**Files:**
- Create: `src/rag/reranker.py`
- Test: `tests/rag/test_reranker.py`

**Interfaces:**
- Consumes: `RERANK_MODEL`, `USE_GPU` (config).
- Produces:
  - `class CrossEncoderReranker` với `__init__(self, model_name=RERANK_MODEL, predictor=None)` và `score(self, query: str, texts: list[str]) -> list[float]` — trả điểm ∈ [0,1] (sigmoid của logit) theo đúng thứ tự `texts`; `texts` rỗng → `[]`; lỗi model → `[]`. `predictor` là callable tiêm-được `list[[q,t]] -> list[float]` (logits) cho test/DI.
  - `get_reranker() -> CrossEncoderReranker` — singleton lazy dùng chung.

- [ ] **Step 1: Write the failing test**

```python
# tests/rag/test_reranker.py
import math
from src.rag.reranker import CrossEncoderReranker, get_reranker


def test_score_applies_sigmoid_in_order():
    r = CrossEncoderReranker(predictor=lambda pairs: [2.0, -2.0, 0.0])
    out = r.score("q", ["a", "b", "c"])
    assert len(out) == 3
    # sigmoid: 2.0 -> ~0.88, -2.0 -> ~0.12, 0.0 -> 0.5, order preserved
    assert out[0] > out[2] > out[1]
    assert math.isclose(out[2], 0.5, abs_tol=1e-6)
    assert all(0.0 <= s <= 1.0 for s in out)


def test_empty_texts_returns_empty():
    r = CrossEncoderReranker(predictor=lambda pairs: [1.0])
    assert r.score("q", []) == []


def test_predictor_error_returns_empty():
    def boom(pairs):
        raise RuntimeError("model down")
    r = CrossEncoderReranker(predictor=boom)
    assert r.score("q", ["a", "b"]) == []


def test_get_reranker_is_singleton():
    assert get_reranker() is get_reranker()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/rag/test_reranker.py -v`
Expected: FAIL (module `src.rag.reranker` not found).

- [ ] **Step 3: Write minimal implementation**

```python
# src/rag/reranker.py
"""Shared cross-encoder reranker (BAAI/bge-reranker-v2-m3), GPU/CPU-safe.

Lazy singleton reused by both text (RerankedRetriever) and image
(ImageVectorDB._rerank) sides. Scores are sigmoid(logit) in [0,1]; any load
or inference failure returns [] so callers fall back to their non-reranked
order instead of crashing the request.
"""
import logging
import math
from typing import Callable, List, Optional

from ..config import RERANK_MODEL, USE_GPU

logger = logging.getLogger(__name__)


def _sigmoid(x: float) -> float:
    try:
        return 1.0 / (1.0 + math.exp(-float(x)))
    except OverflowError:
        return 0.0 if x < 0 else 1.0


class CrossEncoderReranker:
    def __init__(self, model_name: str = RERANK_MODEL,
                 predictor: Optional[Callable[[list], List[float]]] = None):
        self._model_name = model_name
        self._predictor = predictor          # injected for tests/DI
        self._model = None

    def _predict(self, pairs: list) -> List[float]:
        if self._predictor is not None:
            return list(self._predictor(pairs))
        if self._model is None:
            from sentence_transformers import CrossEncoder
            device = "cpu"
            if USE_GPU:
                try:
                    import torch
                    if torch.cuda.is_available():
                        device = "cuda"
                except Exception:
                    device = "cpu"
            logger.info("Loading reranker %s on %s", self._model_name, device)
            self._model = CrossEncoder(self._model_name, device=device, max_length=512)
        return list(self._model.predict(pairs))

    def score(self, query: str, texts: List[str]) -> List[float]:
        if not texts:
            return []
        try:
            pairs = [[query, t] for t in texts]
            logits = self._predict(pairs)
            if len(logits) != len(texts):
                logger.warning("Reranker returned %d scores for %d texts", len(logits), len(texts))
                return []
            return [_sigmoid(x) for x in logits]
        except Exception as e:
            logger.warning("Reranker scoring failed: %s", e)
            return []


_RERANKER: Optional[CrossEncoderReranker] = None


def get_reranker() -> CrossEncoderReranker:
    global _RERANKER
    if _RERANKER is None:
        _RERANKER = CrossEncoderReranker()
    return _RERANKER
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/rag/test_reranker.py -v` → Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/rag/reranker.py tests/rag/test_reranker.py
git commit -m "feat(rag): shared GPU/CPU-safe cross-encoder reranker (M2 task 2)"
```

> **Adversarial note:** `sentence-transformers` CrossEncoder có thể tự áp activation tuỳ model → điểm có thể đã ∈[0,1]; ta vẫn bọc `_sigmoid` (đơn điệu) nên **thứ hạng không đổi**, chỉ dịch ngưỡng tuyệt đối → `RERANK_SCORE_MIN` phải tune theo output thực của `score()` (Task 8). Guard `len(logits)!=len(texts)` → `[]` để caller fallback.

---

### Task 3: `RerankedRetriever` + toggle trong `VectorDB.get_retriever`

**Files:**
- Modify: `src/rag/vectorstore.py` (thêm class `RerankedRetriever`; sửa `VectorDB.get_retriever`)
- Test: `tests/rag/test_reranked_retriever.py`

**Interfaces:**
- Consumes: `CrossEncoderReranker.score` (Task 2), `get_reranker` (Task 2); `RERANK_ENABLED`, `RERANK_FETCH_K`, `RERANK_SCORE_MIN`, `RETRIEVER_MAX_K`, `RETRIEVER_FETCH_K` (config); `similarity_search_with_score(query, k)` của Chroma.
- Produces: `RerankedRetriever(BaseRetriever)` với fields `vectorstore`, `reranker=None`, `fetch_k=RERANK_FETCH_K`, `max_k=RETRIEVER_MAX_K`, `score_min=RERANK_SCORE_MIN`; `VectorDB.get_retriever(search_kwargs)` trả `RerankedRetriever` khi `RERANK_ENABLED` else `RelevanceGatedRetriever`.

- [ ] **Step 1: Write the failing test**

```python
# tests/rag/test_reranked_retriever.py
from langchain_core.documents import Document
from src.rag.vectorstore import RerankedRetriever


class _FakeStore:
    """Mimics Chroma.similarity_search_with_score(query, k) -> [(doc, distance)]."""
    def __init__(self, pairs):
        self._pairs = pairs
    def similarity_search_with_score(self, query, k):
        return self._pairs[:k]


class _FakeReranker:
    def __init__(self, scores):
        self._scores = scores
    def score(self, query, texts):
        # map by page_content marker "d0".."dn" set up in the test
        return [self._scores[t] for t in texts]


def _doc(name):
    return Document(page_content=name, metadata={"source": "s.pdf", "page": 1})


def test_keeps_top_max_k_by_rerank_score():
    # distance order says A best, but reranker says C > A > B
    pairs = [(_doc("A"), 0.1), (_doc("B"), 0.2), (_doc("C"), 0.3)]
    rr = RerankedRetriever(
        vectorstore=_FakeStore(pairs),
        reranker=_FakeReranker({"A": 0.7, "B": 0.3, "C": 0.9}),
        fetch_k=3, max_k=2, score_min=0.2,
    )
    docs = rr.invoke("q")
    assert [d.page_content for d in docs] == ["C", "A"]      # top-2 by rerank
    assert docs[0].metadata["rerank_score"] == 0.9


def test_drops_below_score_min():
    pairs = [(_doc("A"), 0.1), (_doc("B"), 0.2)]
    rr = RerankedRetriever(
        vectorstore=_FakeStore(pairs),
        reranker=_FakeReranker({"A": 0.5, "B": 0.05}),
        fetch_k=2, max_k=4, score_min=0.2,
    )
    docs = rr.invoke("q")
    assert [d.page_content for d in docs] == ["A"]           # B gated out


def test_all_below_min_returns_empty():
    pairs = [(_doc("A"), 0.1)]
    rr = RerankedRetriever(
        vectorstore=_FakeStore(pairs),
        reranker=_FakeReranker({"A": 0.01}),
        fetch_k=1, max_k=4, score_min=0.2,
    )
    assert rr.invoke("q") == []


def test_reranker_failure_falls_back_to_distance_order():
    pairs = [(_doc("A"), 0.3), (_doc("B"), 0.1)]   # B closer by distance

    class _Empty:
        def score(self, query, texts):
            return []                              # model failed
    rr = RerankedRetriever(vectorstore=_FakeStore(pairs), reranker=_Empty(),
                           fetch_k=2, max_k=1, score_min=0.2)
    docs = rr.invoke("q")
    assert [d.page_content for d in docs] == ["B"]           # distance fallback
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/rag/test_reranked_retriever.py -v`
Expected: FAIL (`RerankedRetriever` not defined).

- [ ] **Step 3: Write minimal implementation**

Thêm import ở đầu `src/rag/vectorstore.py`:

```python
from typing import Any
from .reranker import get_reranker
from ..config import RERANK_ENABLED, RERANK_FETCH_K, RERANK_SCORE_MIN
```

Thêm class (sau `RelevanceGatedRetriever`):

```python
class RerankedRetriever(BaseRetriever):
    """Fetch wide, cross-encoder rerank, keep top max_k, gate on rerank score.

    Cross-encoder relevance (higher = better) replaces embedding distance for
    the final ordering. An absolute score gate (score_min) drops weak chunks so
    an all-irrelevant fetch returns nothing and the LLM emits its fallback. If
    the reranker yields no usable scores, fall back to distance order.
    """

    model_config = ConfigDict(arbitrary_types_allowed=True)

    vectorstore: Any
    reranker: Any = None
    fetch_k: int = RERANK_FETCH_K
    max_k: int = RETRIEVER_MAX_K
    score_min: float = RERANK_SCORE_MIN

    def _get_relevant_documents(
        self, query: str, *, run_manager: CallbackManagerForRetrieverRun
    ) -> List[Document]:
        scored = self.vectorstore.similarity_search_with_score(query, k=self.fetch_k)
        if not scored:
            return []
        docs = [doc for doc, _ in scored]
        reranker = self.reranker or get_reranker()
        ce = reranker.score(query, [doc.page_content for doc in docs])

        if not ce or len(ce) != len(docs):
            scored_sorted = sorted(scored, key=lambda pair: pair[1])
            logger.warning("RerankedRetriever: reranker unavailable, distance fallback")
            return [doc for doc, _ in scored_sorted[: self.max_k]]

        paired = sorted(zip(docs, ce), key=lambda pair: pair[1], reverse=True)
        kept: List[Document] = []
        for doc, score in paired:
            if score < self.score_min:
                break
            doc.metadata["rerank_score"] = round(float(score), 4)
            kept.append(doc)
            if len(kept) >= self.max_k:
                break
        logger.info(
            "RerankedRetriever: %d/%d kept (top=%.3f, min=%.2f) for query=%r",
            len(kept), len(docs), paired[0][1], self.score_min, query,
        )
        return kept
```

Sửa `VectorDB.get_retriever`:

```python
    def get_retriever(self, search_kwargs: dict = None):
        search_kwargs = search_kwargs or {}
        max_k = search_kwargs.get("k", RETRIEVER_MAX_K)
        if RERANK_ENABLED:
            return RerankedRetriever(
                vectorstore=self.db,
                fetch_k=max(RERANK_FETCH_K, max_k),
                max_k=max_k,
            )
        return RelevanceGatedRetriever(
            vectorstore=self.db,
            fetch_k=max(RETRIEVER_FETCH_K, max_k),
            max_k=max_k,
        )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/rag/test_reranked_retriever.py -v` → Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/rag/vectorstore.py tests/rag/test_reranked_retriever.py
git commit -m "feat(rag): RerankedRetriever + get_retriever toggle (M2 task 3)"
```

> **Adversarial note:** `paired` sort giảm dần theo score (cross-encoder cao = liên quan) — KHÔNG nhầm chiều với distance. Khi score bằng nhau, `sorted` giữ ổn định thứ tự fetch (distance asc) — chấp nhận được. `doc.metadata` bị mutate tại chỗ (thêm `rerank_score`) — an toàn vì doc là bản trả về của truy vấn, không chia sẻ. Fallback chỉ kích hoạt khi `ce` rỗng/sai độ dài, không khi mọi score < min (trường hợp đó trả rỗng đúng ý).

---

### Task 4: `citations.py` — build citation xác định + tên mục sidebar

**Files:**
- Create: `src/rag/citations.py`
- Test: `tests/rag/test_citations.py`

**Interfaces:**
- Consumes: `get_pdf_variant` (`src.etl.image_processor`), `strip_accents` (`src.rag.query_intent`).
- Produces:
  - `format_book_name(source: str) -> str`
  - `build_citations(docs: list[Document]) -> list[dict]` — mỗi dict `{book, page, section, display}`, dedup theo `(book, page, section)`, sort theo `(book, page)`.
  - `format_citations_block(citations: list[dict]) -> str` — khối "📚 Nguồn:\n- ..." (rỗng nếu list rỗng).
  - `is_fallback_answer(answer: str) -> bool` — True khi answer là câu "không được đề cập".

- [ ] **Step 1: Write the failing test**

```python
# tests/rag/test_citations.py
from langchain_core.documents import Document
from src.rag.citations import (
    format_book_name, build_citations, format_citations_block, is_fallback_answer,
)


def _doc(source="SGK KHTN 7 CTST.pdf", page=40, region_type="body", text="Nội dung."):
    return Document(page_content=text, metadata={
        "source": source, "page": page, "region_type": region_type})


def test_format_book_name_parenthesizes_publisher():
    assert format_book_name("SGK KHTN 7 CTST.pdf") == "SGK KHTN 7 (CTST)"
    assert format_book_name("SGK KHTN 8 KNTT.pdf") == "SGK KHTN 8 (KNTT)"


def test_body_citation_has_no_section():
    cits = build_citations([_doc()])
    assert cits[0]["display"] == "SGK KHTN 7 (CTST), tr. 40"
    assert cits[0]["section"] is None


def test_sidebar_section_extracted_from_first_line():
    d = _doc(region_type="sidebar", text="Em có biết?\nCá voi là thú.")
    cits = build_citations([d])
    assert cits[0]["display"] == 'SGK KHTN 7 (CTST), tr. 40 — mục "Em có biết"'


def test_sidebar_generic_label_when_no_keyword():
    d = _doc(region_type="info_box", text="Một đoạn bất kỳ không có tiêu đề.")
    cits = build_citations([d])
    assert cits[0]["display"].endswith('— mục "khung thông tin"')


def test_dedup_same_book_page_section():
    cits = build_citations([_doc(), _doc(), _doc(page=12)])
    assert len(cits) == 2
    assert [c["page"] for c in cits] == [12, 40]      # sorted by (book, page)


def test_format_block_and_empty():
    cits = build_citations([_doc()])
    block = format_citations_block(cits)
    assert block.startswith("📚 Nguồn:")
    assert "- SGK KHTN 7 (CTST), tr. 40" in block
    assert format_citations_block([]) == ""


def test_is_fallback_answer_matches_normalized():
    assert is_fallback_answer("Thông tin này không được đề cập trong sách giáo khoa.")
    assert is_fallback_answer("... KHÔNG ĐƯỢC ĐỀ CẬP ...")
    assert not is_fallback_answer("Quang hợp là quá trình...")
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/rag/test_citations.py -v`
Expected: FAIL (module not found).

- [ ] **Step 3: Write minimal implementation**

```python
# src/rag/citations.py
"""Deterministic citations built from retrieved-chunk metadata.

Citations are generated from the metadata of chunks that actually fed the
answer — never emitted by the LLM — so page numbers cannot be hallucinated
(the source is a lookup tool for students). Sidebar / info-box chunks carry a
section label ("mục ...") derived from the box's own first line.
"""
import re
from typing import List, Optional

from langchain_core.documents import Document

from ..etl.image_processor import get_pdf_variant
from .query_intent import strip_accents

_PUBLISHER = {"cd": "CD", "ctst": "CTST", "kntt": "KNTT"}

# (accent-free keyword, display label). "cau hoi" also triggers on a "?" line.
_SECTION_KEYWORDS = [
    ("em co biet", "Em có biết"),
    ("cau hoi", "Câu hỏi"),
    ("hoat dong", "Hoạt động"),
    ("luyen tap", "Luyện tập"),
    ("van dung", "Vận dụng"),
    ("tim hieu them", "Tìm hiểu thêm"),
]
_GENERIC_SECTION = {
    "sidebar": "mục bên lề",
    "info_box": "khung thông tin",
    "caption": "chú thích hình",
}


def format_book_name(source: str) -> str:
    stem = re.sub(r"\.pdf$", "", str(source or ""), flags=re.IGNORECASE).strip()
    if not stem:
        return "Sách giáo khoa"
    variant = get_pdf_variant(str(source or ""))
    label = _PUBLISHER.get(variant)
    if label:
        pat = re.compile(rf"[\s_\-]*{variant}\s*$", re.IGNORECASE)
        if pat.search(stem):
            stem = pat.sub("", stem).strip()
            return f"{stem} ({label})"
    return stem


def _section_label(doc: Document) -> Optional[str]:
    metadata = doc.metadata or {}
    region_type = str(metadata.get("region_type") or "body")
    if region_type == "body":
        return None
    content = doc.page_content or ""
    first_line = content.strip().splitlines()[0] if content.strip() else ""
    bare = strip_accents(first_line).lower()
    for keyword, label in _SECTION_KEYWORDS:
        if keyword in bare or (keyword == "cau hoi" and "?" in first_line):
            return label
    return _GENERIC_SECTION.get(region_type)


def _display(book: str, page, section: Optional[str]) -> str:
    base = f"{book}, tr. {page}"
    return f'{base} — mục "{section}"' if section else base


def _page_sort_key(page):
    try:
        return (0, int(page))
    except (TypeError, ValueError):
        return (1, 0)      # unknown pages ("?") sort last


def build_citations(docs: List[Document]) -> List[dict]:
    seen = set()
    out = []
    for doc in docs or []:
        metadata = getattr(doc, "metadata", {}) or {}
        book = format_book_name(metadata.get("source"))
        page = metadata.get("page", metadata.get("page_number", "?"))
        section = _section_label(doc)
        key = (book, str(page), section)
        if key in seen:
            continue
        seen.add(key)
        out.append({"book": book, "page": page, "section": section,
                    "display": _display(book, page, section)})
    out.sort(key=lambda c: (c["book"], _page_sort_key(c["page"])))
    return out


def format_citations_block(citations: List[dict]) -> str:
    if not citations:
        return ""
    lines = "\n".join(f"- {c['display']}" for c in citations)
    return f"📚 Nguồn:\n{lines}"


def is_fallback_answer(answer: str) -> bool:
    bare = strip_accents(str(answer or "")).lower()
    return "khong duoc de cap" in bare
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/rag/test_citations.py -v` → Expected: PASS.
(Nếu `get_pdf_variant` không trả `ctst`/`kntt` như mong đợi cho các tên file này, kiểm hàm thật và điều chỉnh test cho khớp hành vi thật — KHÔNG sửa hàm variant.)

- [ ] **Step 5: Commit**

```bash
git add src/rag/citations.py tests/rag/test_citations.py
git commit -m "feat(rag): deterministic citations with sidebar section labels (M2 task 4)"
```

> **Adversarial note:** dòng đầu box có thể là nhiễu/số trang → chỉ nhận nhãn khi khớp từ khoá, else generic (không bịa). `page` có thể là `"?"` → `_page_sort_key` đẩy xuống cuối, không crash. `format_book_name` chỉ thêm `(NXB)` khi code NXB thực sự xuất hiện cuối tên file (tránh gán nhầm cho file thiếu code).

---

### Task 5: Cắm citation vào `api.py` (chat + SSE)

**Files:**
- Modify: `src/app/api.py` (`prepare_chat_payload` ~97–110; `append_citations` ~113–116; call sites ~201, ~297)
- Test: `tests/app/test_append_citations.py`

**Interfaces:**
- Consumes: `build_citations`, `format_citations_block`, `is_fallback_answer` (Task 4).
- Produces: `prepare_chat_payload` trả `payload["citations"] = build_citations(text_docs)` (thay `citations_str`); `append_citations(answer, citations)` dùng module mới (suppress khi fallback / rỗng).

- [ ] **Step 1: Write the failing test**

```python
# tests/app/test_append_citations.py
from langchain_core.documents import Document
from src.rag.citations import build_citations
from src.app.api import append_citations


def _cits():
    return build_citations([Document(page_content="x", metadata={
        "source": "SGK KHTN 7 CTST.pdf", "page": 40, "region_type": "body"})])


def test_appends_sources_block():
    out = append_citations("Quang hợp là quá trình.", _cits())
    assert "📚 Nguồn:" in out
    assert "SGK KHTN 7 (CTST), tr. 40" in out


def test_suppressed_on_fallback_answer():
    out = append_citations("Thông tin này không được đề cập trong sách giáo khoa.", _cits())
    assert "Nguồn" not in out


def test_no_citations_returns_answer_unchanged():
    assert append_citations("Bất kỳ.", []) == "Bất kỳ."
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/app/test_append_citations.py -v`
Expected: FAIL (`append_citations` cũ nhận `citations_str`, so sánh khác → test format mới fail). Tạo `tests/app/__init__.py` nếu cần.

- [ ] **Step 3: Write minimal implementation**

Thêm import gần đầu `src/app/api.py`:

```python
from src.rag.citations import build_citations, format_citations_block, is_fallback_answer
```

Trong `prepare_chat_payload`, thay khối build citation (dòng ~97–102) — bỏ set `citations`/`citations_str`, và ở `return {"mode": "llm", ...}` đổi:

```python
    return {
        "mode": "llm",
        "formatted_prompt": services.rag.prompt.format(context=context_str, question=question),
        "citations": build_citations(text_docs),
        "images": gallery_items,
        "services": services,
    }
```

Thay `append_citations`:

```python
def append_citations(answer, citations):
    if is_fallback_answer(answer) or not citations:
        return answer
    block = format_citations_block(citations)
    return f"{answer}\n\n{block}" if block else answer
```

Sửa 2 call site cho khớp key mới:
- Stream (~201): `answer = append_citations(parsed_answer, payload["citations"])`
- Non-stream (~297): `answer = append_citations(answer, payload["citations"])`

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/app/test_append_citations.py -v` → Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/app/api.py tests/app/
git commit -m "feat(api): attach deterministic citations to chat + stream (M2 task 5)"
```

> **Adversarial note:** import `src.app.api` kéo theo Flask app + `AppServices` lazy — test chỉ gọi `append_citations` thuần, KHÔNG khởi tạo services (không nạp model). Nếu import-time của `api.py` nạp nặng, cân nhắc test `append_citations` qua import trực tiếp hàm; xác nhận import nhẹ khi chạy test. Các nhánh `mode=="static"` (image-only, không có text) KHÔNG kèm citation — đúng thiết kế.

---

### Task 6: Cross-encoder term cho ảnh (`ImageVectorDB._rerank`)

**Files:**
- Modify: `src/rag/image_vectorstore.py` (thêm `self._reranker` lazy; method `_cross_encoder_boost`; gọi trong `_rerank` sau sort đầu tiên)
- Test: `tests/rag/test_image_cross_encoder.py`

**Interfaces:**
- Consumes: `get_reranker` (Task 2); `IMAGE_RERANK_ENABLED`, `IMAGE_RERANK_TOP_N`, `IMAGE_RERANK_WEIGHT` (config).
- Produces: `ImageVectorDB._cross_encoder_boost(self, query, scored_docs) -> list[Document]` — cộng `IMAGE_RERANK_WEIGHT * ce` vào `image_relevance_score` của top-N rồi sort lại; no-op khi tắt/không reranker. Đọc `self._reranker` (đặt được từ test).

- [ ] **Step 1: Write the failing test** (khởi tạo instance qua `__new__` để KHÔNG nạp CLIP)

```python
# tests/rag/test_image_cross_encoder.py
from langchain_core.documents import Document
from src.rag.image_vectorstore import ImageVectorDB


def _bare_db():
    return ImageVectorDB.__new__(ImageVectorDB)   # bypass heavy __init__ (no CLIP)


def _scored(name, score, search_text=""):
    return Document(page_content=name, metadata={
        "image_relevance_score": score, "search_text": search_text})


class _FakeReranker:
    def __init__(self, mapping):
        self.mapping = mapping
    def score(self, query, texts):
        return [self.mapping[t] for t in texts]


def test_boost_reorders_by_cross_encoder(monkeypatch):
    import src.rag.image_vectorstore as M
    monkeypatch.setattr(M, "IMAGE_RERANK_ENABLED", True)
    monkeypatch.setattr(M, "IMAGE_RERANK_TOP_N", 12)
    monkeypatch.setattr(M, "IMAGE_RERANK_WEIGHT", 0.25)

    db = _bare_db()
    db._reranker = _FakeReranker({"tA": 0.1, "tB": 0.9})
    # B starts slightly behind A but the cross-encoder strongly prefers B
    docs = [_scored("A", 0.50, "tA"), _scored("B", 0.45, "tB")]
    out = db._cross_encoder_boost("q", docs)
    assert [d.page_content for d in out] == ["B", "A"]
    assert out[0].metadata["image_cross_encoder_score"] == 0.9


def test_disabled_is_noop(monkeypatch):
    import src.rag.image_vectorstore as M
    monkeypatch.setattr(M, "IMAGE_RERANK_ENABLED", False)
    db = _bare_db()
    db._reranker = _FakeReranker({"tA": 0.9})
    docs = [_scored("A", 0.5, "tA")]
    out = db._cross_encoder_boost("q", docs)
    assert out[0].metadata.get("image_cross_encoder_score") is None
    assert out[0].metadata["image_relevance_score"] == 0.5


def test_exact_phrase_lead_not_overturned(monkeypatch):
    import src.rag.image_vectorstore as M
    monkeypatch.setattr(M, "IMAGE_RERANK_ENABLED", True)
    monkeypatch.setattr(M, "IMAGE_RERANK_TOP_N", 12)
    monkeypatch.setattr(M, "IMAGE_RERANK_WEIGHT", 0.25)
    db = _bare_db()
    db._reranker = _FakeReranker({"tExact": 0.0, "tOther": 1.0})
    # exact phrase match gave +0.45 lead; max CE delta is 0.25 -> lead holds
    docs = [_scored("Exact", 0.80, "tExact"), _scored("Other", 0.40, "tOther")]
    out = db._cross_encoder_boost("q", docs)
    assert out[0].page_content == "Exact"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/rag/test_image_cross_encoder.py -v`
Expected: FAIL (`_cross_encoder_boost` not defined).

- [ ] **Step 3: Write minimal implementation**

Thêm import trong `src/rag/image_vectorstore.py`:

```python
from ..config import (
    ...,
    IMAGE_RERANK_ENABLED,
    IMAGE_RERANK_TOP_N,
    IMAGE_RERANK_WEIGHT,
)
from .reranker import get_reranker
```

Trong `__init__`, sau khi `_init_chroma`:

```python
        self._reranker = get_reranker() if IMAGE_RERANK_ENABLED else None
```

Thêm method (trong class `ImageVectorDB`):

```python
    def _cross_encoder_boost(self, query: str, scored_docs: List[Document]) -> List[Document]:
        """Add a cross-encoder relevance term to the top-N fused candidates.

        The figure's text identity (search_text / caption) is scored against the
        query and blended into image_relevance_score with a small weight, then
        the list is re-sorted. No-op when disabled or the reranker is missing.
        The weight is deliberately below the exact-phrase bonus (0.45) so an
        exact label match is never overturned by the cross-encoder.
        """
        if not IMAGE_RERANK_ENABLED or not scored_docs:
            return scored_docs
        reranker = getattr(self, "_reranker", None)
        if reranker is None:
            return scored_docs

        top = scored_docs[:IMAGE_RERANK_TOP_N]
        texts = [
            str((d.metadata or {}).get("search_text") or d.page_content or "")
            for d in top
        ]
        ce = reranker.score(query, texts)
        if not ce or len(ce) != len(top):
            return scored_docs
        for doc, s in zip(top, ce):
            metadata = doc.metadata
            base = float(metadata.get("image_relevance_score") or 0.0)
            metadata["image_cross_encoder_score"] = round(float(s), 4)
            metadata["image_relevance_score"] = round(base + IMAGE_RERANK_WEIGHT * float(s), 4)
        scored_docs.sort(
            key=lambda d: d.metadata.get("image_relevance_score", 0.0), reverse=True
        )
        return scored_docs
```

Gọi trong `_rerank`, ngay **sau** dòng `scored_docs.sort(... image_relevance_score ..., reverse=True)` (dòng ~681) và **trước** dòng tính `top_score`:

```python
        scored_docs.sort(key=lambda doc: doc.metadata.get("image_relevance_score", 0.0), reverse=True)
        scored_docs = self._cross_encoder_boost(query, scored_docs)   # <-- thêm dòng này
        top_score = scored_docs[0].metadata.get("image_relevance_score", 0.0) if scored_docs else 0.0
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/rag/test_image_cross_encoder.py -v` → Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/rag/image_vectorstore.py tests/rag/test_image_cross_encoder.py
git commit -m "feat(rag): additive cross-encoder term in image rerank (M2 task 6)"
```

> **Adversarial note:** đặt boost SAU sort đầu và TRƯỚC `top_score` để cửa sổ `score_window`/`min_score` chạy trên điểm đã blend. `IMAGE_RERANK_WEIGHT=0.25 < 0.45` (phrase bonus) → exact match không bị lật (test khẳng định). `len(ce)!=len(top)` → no-op (an toàn). Chỉ boost top-N, phần đuôi giữ nguyên điểm — chấp nhận vì đuôi hiếm khi vào top-k cuối. Kiểm QA ảnh thực ở Task 8 (vài "cho tôi hình con X").

---

### Task 7: Eval — recall@k reranker on/off + MRR

**Files:**
- Modify: `src/test/recall_at_k.py` (thêm chế độ rerank + cột MRR)
- Test: `tests/test_mrr_metric.py`

**Interfaces:**
- Consumes: `VectorDB`, `get_reranker`, `make_page_relevance` (`src.test.metrics`).
- Produces: hàm thuần `reciprocal_rank(rel_flags: list[bool]) -> float` (1/hạng của hit đầu, 0 nếu không có) — dùng cho MRR. Script chạy tay in bảng baseline vs rerank + MRR.

- [ ] **Step 1: Write the failing test**

```python
# tests/test_mrr_metric.py
from src.test.recall_at_k import reciprocal_rank


def test_reciprocal_rank_first_hit():
    assert reciprocal_rank([False, True, False]) == 0.5
    assert reciprocal_rank([True, False]) == 1.0
    assert reciprocal_rank([False, False]) == 0.0
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_mrr_metric.py -v`
Expected: FAIL (`reciprocal_rank` not defined).

- [ ] **Step 3: Write minimal implementation**

Thêm vào `src/test/recall_at_k.py` (trên `main`):

```python
def reciprocal_rank(rel_flags):
    """1/rank của kết quả liên quan đầu tiên (1-based), 0 nếu không có."""
    for i, is_rel in enumerate(rel_flags):
        if is_rel:
            return 1.0 / (i + 1)
    return 0.0


def _rerank_order(query, scored, reranker):
    """Sắp lại (doc,dist) theo cross-encoder; fallback distance nếu reranker rỗng."""
    docs = [d for d, _ in scored]
    ce = reranker.score(query, [d.page_content for d in docs])
    if not ce or len(ce) != len(docs):
        return [d for d, _ in sorted(scored, key=lambda p: p[1])]
    return [d for d, _ in sorted(zip(docs, ce), key=lambda p: p[1], reverse=True)]
```

Trong `main`, sau khi có `ordered` (baseline distance order), tính thêm nhánh rerank cho mỗi câu hỏi và cộng `mrr`. Cụ thể, trong vòng lặp câu hỏi thay phần tính hits bằng:

```python
            scored = db.similarity_search_with_score(q, k=max_k)
            base_docs = [d for d, _ in sorted(scored, key=lambda p: p[1])]
            rer_docs = _rerank_order(q, scored, reranker) if reranker else base_docs
            for tag, docs_ordered in (("base", base_docs), ("rer", rer_docs)):
                flags = [is_rel(d.metadata or {}) for d in docs_ordered]
                for k in KS:
                    if any(flags[:k]):
                        hits[(tag, k)] += 1
                mrr_sum[tag] += reciprocal_rank(flags)
```

Khởi tạo `reranker = get_reranker()` (bọc try/except → None nếu nạp lỗi, in cảnh báo), `hits = {(t, k): 0 for t in ("base","rer") for k in KS}`, `mrr_sum = {"base":0.0,"rer":0.0}`; mở rộng cột report `recall@k (base|rer)` + `MRR (base|rer)`. Giữ nguyên phần ghi CSV/MD (thêm cột).

> Đây là script đo, phần in bảng chạy tay; chỉ hàm `reciprocal_rank` có unit test.

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_mrr_metric.py -v` → Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/test/recall_at_k.py tests/test_mrr_metric.py
git commit -m "feat(eval): recall@k baseline-vs-rerank + MRR (M2 task 7)"
```

> **Adversarial note:** `similarity_search_with_score` chỉ trả `max_k` ứng viên; rerank chỉ đảo trong tập đó (không tăng recall@max_k, nhưng cải thiện recall@k nhỏ và MRR — đúng luận điểm D-08 "nút thắt ở xếp hạng"). Nếu muốn đo trần recall thật, nới k fetch riêng cho rerank — ghi chú, không bắt buộc trong M2.

---

### Task 8: Integration thật (skip-if-offline) + cập nhật tài liệu

**Files:**
- Create: `tests/rag/test_reranker_integration.py`
- Modify: `CLAUDE.md` (mục config/models: thêm reranker + bge-m3), `document/decision_log.html` (đánh dấu M2 code xong khi hoàn tất)
- Create: `document/runbooks/m2_eval.md` (cách đo trên DB bge-m3 sau Colab)

**Interfaces:** none mới — xác thực reranker thật nạp & chấm đúng chiều; tài liệu hoá.

- [ ] **Step 1: Write the integration test (skip khi model/lib vắng)**

```python
# tests/rag/test_reranker_integration.py
import os
import pytest

RUN = os.getenv("RUN_RERANK_INTEGRATION") == "1"


@pytest.mark.skipif(not RUN, reason="set RUN_RERANK_INTEGRATION=1 to load bge-reranker")
def test_real_reranker_orders_relevant_higher():
    from src.rag.reranker import CrossEncoderReranker
    r = CrossEncoderReranker()
    scores = r.score(
        "Quang hợp là gì?",
        ["Quang hợp là quá trình cây xanh tổng hợp chất hữu cơ từ ánh sáng.",
         "Bảng tuần hoàn các nguyên tố hóa học."],
    )
    assert len(scores) == 2
    assert scores[0] > scores[1]
```

- [ ] **Step 2: Run it (offline)** — `python -m pytest tests/rag/test_reranker_integration.py -v` → Expected: SKIPPED (khi chưa set env). Khi có model + `RUN_RERANK_INTEGRATION=1`: PASS.

- [ ] **Step 3: Cập nhật tài liệu**
  - `CLAUDE.md`: trong "Key conventions"/models, ghi: embedding text mặc định `BAAI/bge-m3`; thêm reranker `BAAI/bge-reranker-v2-m3` (cờ `RERANK_ENABLED`, `IMAGE_RERANK_ENABLED`); citation xác định trong `api.py` + `src/rag/citations.py`.
  - `document/runbooks/m2_eval.md`: các bước (1) đảm bảo DB bge-m3 đã dựng (chạy `--text-only` trên Colab, D-18); (2) `python src/test/recall_at_k.py` đọc bảng base-vs-rer + MRR; (3) tune `RERANK_SCORE_MIN` nếu quá nhiều "không được đề cập"; (4) QA ảnh: `python src/test/test_image_extraction_full.py` + vài truy vấn "cho tôi hình con X" kiểm cross-encoder không lật exact match; (5) ghi số chốt vào decision log.

- [ ] **Step 4: Chạy focused test-suite của M2** — `python -m pytest tests/rag/ tests/app/ tests/test_mrr_metric.py -v` → Expected: PASS (integration SKIPPED).

- [ ] **Step 5: Commit**

```bash
git add tests/rag/test_reranker_integration.py CLAUDE.md document/runbooks/m2_eval.md document/decision_log.html
git commit -m "test(rag): real-reranker integration (skip-if-offline) + M2 docs (M2 task 8)"
```

> **Adversarial note:** integration mặc định SKIP để CI/local không kéo model nặng; chỉ bật thủ công. Đây là cổng xác nhận cross-encoder đúng chiều trên câu thật trước khi tin số eval.

---

## Self-Review

**Spec coverage:**
- C1 embedding bge-m3 → Task 1. C2 reranker dùng chung → Task 2. C3 RerankedRetriever + toggle → Task 3. C4 image cross-encoder term → Task 6. C5 citation xác định + tên mục + suppress → Task 4 (+ cắm api.py Task 5). C6 prompt giữ nguyên → không cần task (no-op, ghi rõ). C7 eval reranker on/off + MRR → Task 7. Device-safe D-12 → Task 2. Integration/docs → Task 8.

**Placeholder scan:** mọi step code có code thật; Task 7 phần in bảng và Task 8 phần tài liệu là thao tác tay nhưng có bước cụ thể. Không "TBD".

**Type consistency:** `CrossEncoderReranker.score(query, texts)->list[float]` dùng nhất quán ở Task 3/6/7; `get_reranker()` singleton dùng ở Task 3/6/7; `build_citations()->list[dict]`/`format_citations_block(list[dict])`/`is_fallback_answer(str)` khớp giữa Task 4 và Task 5; `_cross_encoder_boost(query, scored_docs)` chỉ dùng nội bộ Task 6.

**Known follow-ups (không chặn M2):** tune `RERANK_SCORE_MIN`/`IMAGE_RERANK_WEIGHT` cần DB bge-m3 (sau Colab, Task 8 runbook); `chain.py` LCEL để nguyên (không nằm trên đường chat); `format_book_name` chỉ chuẩn hoá NXB khi code xuất hiện cuối tên file.
