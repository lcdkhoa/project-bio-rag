# M3 — Figure extraction (layout reconcile) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cắt hình đúng hơn bằng cách **tái dùng** bộ detect anchor-first per-variant (`image_processor.py`) và **chèn một bước reconcile** dùng vùng box của layout segmenter để loại figure dương-tính-giả (sidebar/info-box bị box nhầm thành figure), giữ nguyên schema metadata → `biology_images`/`biology_image_metadata`, caption Vintern, CLIP, và vòng human-review.

**Architecture:** Một module bridge mới `src/etl/layout/figure_bridge.py` cung cấp `reconcile_with_layout()` (drop-only, bảo thủ, chỉ áp cho region **kiểu figure**) + `to_layout_regions()` (cho QA overlay). `image_processor.extract_images_from_pdf` gọi reconcile qua **local import** ngay sau `detect_regions_anchor_first`. Entrypoint ETL (`main.py`) đổi sang `make_image_processor(filename)` **theo từng cuốn** (sửa lỗ hổng routing base-class). Kèm bump `IMAGE_EXTRACTION_VERSION` để phá cache và một fix `đ`-fold nhỏ trong kênh lexical ảnh.

**Tech Stack:** Python, OpenCV (`cv2`), PyMuPDF/poppler render (150 DPI), Tesseract (qua segmenter), ChromaDB/CLIP (không đổi), pytest. Test nhỏ + fixture tổng hợp; QA ảnh bằng công cụ trực quan.

**Spec:** `document/specs/2026-08-19-m3-figure-extraction-design.md`

## Global Constraints

- **Windows-primary dev.** Chạy test: `python -m pytest <path> -v` từ repo root (worktree `D:\personal_repo\project_rag_m3`).
- **Chỉ chạy test tập trung của phần đang sửa khi lặp** (CLAUDE.md). Full suite chỉ khi đóng milestone.
- **Không nạp model thật trong unit test.** `segment_page` (OpenCV, nhẹ) được **monkeypatch** trong test bridge để kiểm logic thuần; ImageProcessor `__init__` nạp `ImageCaptioner`/`ProcessingStatus` → **monkeypatch** khi test factory.
- **Commit messages: KHÔNG có `Co-Authored-By` / "Generated with".** Plain messages.
- **REUSE, không rewrite:** KHÔNG sửa `detect_regions_anchor_first` hay builder per-variant (CTST band, KNTT pill, sub-figure splitter). Reconcile chỉ **bớt** region, không thêm/sửa bbox.
- **Không gian toạ độ (trục chống-bug):** detector chạy **poppler 150-DPI, RGB**. Bridge gọi `segment_page` trên **chính mảng đó**, đổi **RGB→BGR** trước — KHÔNG re-render ở DPI khác.
- **Drop-only, bảo thủ:** chỉ drop figure có **containment** (dt giao / dt figure) ≥ `FIGURE_IN_BOX_DROP_RATIO` (0.80) vào một box; KHÔNG clip. **Chỉ áp cho region kiểu figure** (`single_figure`/`composite_figure`/`sub_figure`/`panel`) — KHÔNG bao giờ drop `textbook_info_box`/`activity_box`/`tool_group` (đó là box hợp lệ).
- **Bump cache:** `IMAGE_EXTRACTION_VERSION` default mới `v16_layout_reconcile` (override qua `.env`).
- Config đọc từ `.env`/`src/config.py`, không hardcode. Giá trị mặc định (verbatim): `FIGURE_IN_BOX_DROP_RATIO=0.80`, `IMAGE_EXTRACTION_VERSION=v16_layout_reconcile`.
- **Phản biện chống bug ẩn trước khi báo done** (D-06): lệch toạ độ 150/220 DPI, đảo RGB/BGR, chiều containment, drop nhầm radial/info-box, cache không bump, cycle import.

---

### Task 1: Config — `FIGURE_IN_BOX_DROP_RATIO` + version bump

**Files:**
- Modify: `src/config.py` (dòng 82–83 `IMAGE_EXTRACTION_VERSION`; thêm block M3 sau dòng ~135 khối Layout M1)
- Test: `tests/layout/test_config_m3.py`

**Interfaces:**
- Produces: `FIGURE_IN_BOX_DROP_RATIO: float` (=0.80), `IMAGE_EXTRACTION_VERSION: str` default = `"v16_layout_reconcile"`.

- [ ] **Step 1: Write the failing test**

```python
# tests/layout/test_config_m3.py
import importlib


def test_m3_config_defaults(monkeypatch):
    for key in ["IMAGE_EXTRACTION_VERSION", "FIGURE_IN_BOX_DROP_RATIO"]:
        monkeypatch.delenv(key, raising=False)
    import src.config as cfg
    importlib.reload(cfg)

    assert cfg.IMAGE_EXTRACTION_VERSION == "v16_layout_reconcile"
    assert cfg.FIGURE_IN_BOX_DROP_RATIO == 0.80
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/layout/test_config_m3.py -v`
Expected: FAIL (version vẫn `v15_per_variant`; `FIGURE_IN_BOX_DROP_RATIO` chưa có).

- [ ] **Step 3: Write minimal implementation**

Sửa dòng 82–83 trong `src/config.py`:

```python
IMAGE_EXTRACTION_VERSION = os.getenv(
    "IMAGE_EXTRACTION_VERSION", "v16_layout_reconcile")
```

Thêm vào cuối khối Layout M1 (sau dòng `LAYOUT_BOX_MIN_AREA_FRAC`, ~135):

```python
# --- M3: figure extraction (layout reconcile) ---
# Drop a FIGURE region only when this fraction of its area lies inside a
# segmenter colour box (sidebar/info-box). Containment (intersection / figure
# area), NOT symmetric IoU — a large box must not dilute the signal. High +
# drop-only so a radial figure with coloured icons is never eaten.
FIGURE_IN_BOX_DROP_RATIO = float(os.getenv("FIGURE_IN_BOX_DROP_RATIO", "0.80"))
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/layout/test_config_m3.py -v` → Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/config.py tests/layout/test_config_m3.py
git commit -m "feat(config): FIGURE_IN_BOX_DROP_RATIO + image extraction version bump (M3 task 1)"
```

> **Adversarial note:** bump version là điều bắt buộc để checkpoint versioned re-extract mọi trang (không đổi version = crop logic mới nhưng trang cũ bị skip → QA sai). `importlib.reload(cfg)` chỉ khẳng định default; module đã import hằng số cũ không tự cập nhật (bình thường, app đọc lúc import).

---

### Task 2: `figure_bridge.reconcile_with_layout` + `_containment`

**Files:**
- Create: `src/etl/layout/figure_bridge.py`
- Test: `tests/layout/test_figure_bridge.py`

**Interfaces:**
- Consumes: `segment_page` (`src.etl.layout.segmenter`), `Region`/`RegionType` (`src.etl.layout.regions`), `FIGURE_IN_BOX_DROP_RATIO` (config).
- Produces:
  - `reconcile_with_layout(regions: list[dict], image_rgb: np.ndarray, variant: str) -> list[dict]` — trả danh sách region đã lọc; drop figure-type region có containment ≥ ratio vào một box; giữ nguyên mọi region khác. Lỗi segmentation → trả nguyên (fail-open). `regions` rỗng → trả nguyên.
  - `_containment(fig_bbox, box_bbox) -> float` — dt(giao)/dt(figure) ∈ [0,1].
  - Hằng module `_FIGURE_TYPES = {"single_figure", "composite_figure", "sub_figure", "panel", "figure"}`.

- [ ] **Step 1: Write the failing test**

```python
# tests/layout/test_figure_bridge.py
import numpy as np
import src.etl.layout.figure_bridge as FB
from src.etl.layout.regions import Region, RegionType


def _fig(bbox, image_type="single_figure"):
    return {"bbox": bbox, "image_type": image_type}


def _patch_boxes(monkeypatch, boxes):
    """Make segment_page return SIDEBAR regions for the given bboxes."""
    captured = {}

    def fake_segment(image, variant):
        captured["image"] = image
        captured["variant"] = variant
        regs = [Region(RegionType.BODY, (0, 0, 10, 10), 0, {})]
        regs += [Region(RegionType.SIDEBAR, b, i + 1, {}) for i, b in enumerate(boxes)]
        return regs

    monkeypatch.setattr(FB, "segment_page", fake_segment)
    return captured


def test_containment_ratio():
    # figure fully inside box -> 1.0 ; half inside -> 0.5 ; disjoint -> 0.0
    assert FB._containment((10, 10, 20, 20), (0, 0, 100, 100)) == 1.0
    assert FB._containment((0, 0, 10, 10), (5, 0, 100, 10)) == 0.5
    assert FB._containment((0, 0, 10, 10), (50, 50, 60, 60)) == 0.0


def test_drops_figure_inside_box(monkeypatch):
    _patch_boxes(monkeypatch, [(0, 0, 100, 100)])
    regions = [_fig((10, 10, 90, 90))]              # fully inside the box
    out = FB.reconcile_with_layout(regions, np.zeros((120, 120, 3), np.uint8), "cd")
    assert out == []


def test_keeps_marginally_overlapping_figure(monkeypatch):
    _patch_boxes(monkeypatch, [(0, 0, 100, 100)])
    regions = [_fig((80, 80, 300, 300))]            # mostly outside the box
    out = FB.reconcile_with_layout(regions, np.zeros((320, 320, 3), np.uint8), "cd")
    assert out == regions


def test_keeps_figure_when_no_boxes(monkeypatch):
    _patch_boxes(monkeypatch, [])
    regions = [_fig((10, 10, 90, 90))]
    out = FB.reconcile_with_layout(regions, np.zeros((120, 120, 3), np.uint8), "cd")
    assert out == regions


def test_never_drops_info_or_activity_box(monkeypatch):
    _patch_boxes(monkeypatch, [(0, 0, 100, 100)])
    # these ARE colour boxes by design; being inside a box must NOT drop them
    regions = [_fig((10, 10, 90, 90), image_type="textbook_info_box"),
               _fig((10, 10, 90, 90), image_type="activity_box"),
               _fig((10, 10, 90, 90), image_type="tool_group")]
    out = FB.reconcile_with_layout(regions, np.zeros((120, 120, 3), np.uint8), "cd")
    assert out == regions


def test_feeds_segmenter_bgr_view(monkeypatch):
    captured = _patch_boxes(monkeypatch, [])
    img = np.zeros((4, 4, 3), np.uint8)
    img[..., 0] = 255       # pure RED in RGB (channel 0)
    FB.reconcile_with_layout([_fig((0, 0, 1, 1))], img, "ctst")
    # bridge must hand segment_page the BGR view: red ends up in channel 2
    assert captured["image"][0, 0, 2] == 255
    assert captured["image"][0, 0, 0] == 0
    assert captured["variant"] == "ctst"


def test_segmentation_error_is_fail_open(monkeypatch):
    def boom(image, variant):
        raise RuntimeError("cv exploded")
    monkeypatch.setattr(FB, "segment_page", boom)
    regions = [_fig((10, 10, 90, 90))]
    out = FB.reconcile_with_layout(regions, np.zeros((120, 120, 3), np.uint8), "cd")
    assert out == regions
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/layout/test_figure_bridge.py -v`
Expected: FAIL (module `src.etl.layout.figure_bridge` not found).

- [ ] **Step 3: Write minimal implementation**

```python
# src/etl/layout/figure_bridge.py
"""Bridge: reconcile anchor-first figure regions against the layout segmenter.

The per-variant detector in image_processor.py stays the source of figure
regions (all v7-v15 logic intact). This bridge only *removes* figure regions
that the segmenter shows are actually a coloured sidebar/info box — a
conservative, drop-only reconciliation. It never clips or grows a figure, so a
radial figure whose coloured icons might trip the box detector is protected by
a high containment threshold (a real figure is not ~entirely inside one box).

Only FIGURE-typed regions are drop candidates: the detector also emits
textbook_info_box / activity_box / tool_group regions that ARE coloured boxes
by design and must never be dropped.
"""
import logging
from typing import List

import cv2
import numpy as np

from .regions import Region, RegionType
from .segmenter import segment_page
from ...config import FIGURE_IN_BOX_DROP_RATIO

logger = logging.getLogger(__name__)

_BOX_TYPES = (RegionType.SIDEBAR, RegionType.INFO_BOX)
_FIGURE_TYPES = {"single_figure", "composite_figure", "sub_figure", "panel", "figure"}


def _containment(fig_bbox, box_bbox) -> float:
    """Fraction of the FIGURE's area that lies inside box_bbox (0..1)."""
    fx0, fy0, fx1, fy1 = fig_bbox
    bx0, by0, bx1, by1 = box_bbox
    fig_area = max(0, fx1 - fx0) * max(0, fy1 - fy0)
    if fig_area <= 0:
        return 0.0
    ix0, iy0 = max(fx0, bx0), max(fy0, by0)
    ix1, iy1 = min(fx1, bx1), min(fy1, by1)
    inter = max(0, ix1 - ix0) * max(0, iy1 - iy0)
    return inter / fig_area


def reconcile_with_layout(regions: List[dict], image_rgb: np.ndarray, variant: str) -> List[dict]:
    """Drop figure-type regions sitting ~entirely inside a segmenter colour box.

    ``image_rgb`` is the detector's own page array (poppler, RGB); segment_page
    is fed the BGR view of that very array so the boxes share the SAME
    coordinate space as ``regions`` (no re-render). Fail-open: any segmentation
    error keeps all regions.
    """
    if not regions:
        return regions
    try:
        image_bgr = cv2.cvtColor(image_rgb, cv2.COLOR_RGB2BGR)
        layout = segment_page(image_bgr, variant)
    except Exception as e:
        logger.warning("reconcile_with_layout: segmentation failed (%s); keeping all regions", e)
        return regions
    boxes = [r.bbox for r in layout if r.type in _BOX_TYPES]
    if not boxes:
        return regions

    kept: List[dict] = []
    for region in regions:
        image_type = str(region.get("image_type", "")).lower()
        bbox = tuple(region["bbox"])
        if image_type in _FIGURE_TYPES and any(
            _containment(bbox, box) >= FIGURE_IN_BOX_DROP_RATIO for box in boxes
        ):
            logger.info("reconcile: drop %s figure %s inside colour box", image_type, bbox)
            continue
        kept.append(region)
    return kept


def to_layout_regions(regions: List[dict]) -> List[Region]:
    """Represent detector regions as layout Region objects for the QA overlay.

    Figure types -> RegionType.FIGURE; everything else (info/activity/tool) ->
    RegionType.INFO_BOX. The original label is carried in meta["image_type"].
    """
    out: List[Region] = []
    for i, region in enumerate(regions):
        image_type = str(region.get("image_type", "")).lower()
        rtype = RegionType.FIGURE if image_type in _FIGURE_TYPES else RegionType.INFO_BOX
        out.append(Region(rtype, tuple(region["bbox"]), reading_order=i,
                          meta={"image_type": image_type}))
    return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/layout/test_figure_bridge.py -v` → Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/etl/layout/figure_bridge.py tests/layout/test_figure_bridge.py
git commit -m "feat(etl): layout figure_bridge — drop-only reconcile of figures vs colour boxes (M3 task 2)"
```

> **Adversarial note:** containment dùng dt-figure ở mẫu số → box lớn KHÔNG làm loãng; ngưỡng 0.80 + chỉ figure-type bảo vệ radial (icon spoke tạo box nhỏ, containment thấp) và info/activity box (bị loại khỏi drop bằng `_FIGURE_TYPES`). `test_feeds_segmenter_bgr_view` chốt đúng chiều màu — bug đảo RGB/BGR sẽ fail. Fail-open đảm bảo reconcile không bao giờ làm mất region khi segmenter lỗi.

---

### Task 3: `to_layout_regions` test + cắm reconcile vào `extract_images_from_pdf`

**Files:**
- Modify: `src/etl/image_processor.py` (trong `extract_images_from_pdf`, ngay sau `detected_regions = detection["regions"]`, ~dòng 3793)
- Test: `tests/layout/test_figure_bridge.py` (thêm test cho `to_layout_regions`)

**Interfaces:**
- Consumes: `reconcile_with_layout` (Task 2), `get_pdf_variant` (`src.etl.image_processor`).
- Produces: `extract_images_from_pdf` lọc `detected_regions` qua reconcile trước khi crop/metadata; `to_layout_regions` map đúng type.

- [ ] **Step 1: Write the failing test** (thêm vào `tests/layout/test_figure_bridge.py`)

```python
def test_to_layout_regions_maps_types():
    regions = [{"bbox": (0, 0, 10, 10), "image_type": "composite_figure"},
               {"bbox": (0, 0, 10, 10), "image_type": "activity_box"}]
    out = FB.to_layout_regions(regions)
    assert out[0].type is RegionType.FIGURE
    assert out[1].type is RegionType.INFO_BOX
    assert out[0].meta["image_type"] == "composite_figure"
    assert [r.reading_order for r in out] == [0, 1]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/layout/test_figure_bridge.py::test_to_layout_regions_maps_types -v`
Expected: PASS nếu Task 2 đã có `to_layout_regions` (nó đã nằm trong impl Task 2). Nếu Task 2 chưa thêm map INFO_BOX đúng, FAIL → sửa. (Test này chốt hợp đồng map trước khi dùng ở QA Task 6.)

- [ ] **Step 3: Cắm reconcile vào `extract_images_from_pdf`**

Trong `src/etl/image_processor.py`, tìm (trong `extract_images_from_pdf`):

```python
            detected_regions: List[Dict[str, object]] = detection["regions"]
```

Chèn NGAY SAU dòng đó:

```python
            # M3: drop figure regions the layout segmenter shows are actually a
            # colour sidebar/info box. Local import avoids an image_processor <->
            # layout import cycle. Same 150-DPI/RGB array the detector used.
            from .layout.figure_bridge import reconcile_with_layout
            detected_regions = reconcile_with_layout(
                detected_regions, img_array, get_pdf_variant(pdf_filename))
```

(`img_array` và `pdf_filename` đều có sẵn trong scope vòng lặp; `get_pdf_variant` đã import ở cuối file cùng module — dùng trực tiếp.)

- [ ] **Step 4: Run to verify** — `python -m pytest tests/layout/test_figure_bridge.py -v` → PASS. Import sanity: `python -c "import src.etl.image_processor"` chạy không lỗi cycle.

- [ ] **Step 5: Commit**

```bash
git add src/etl/image_processor.py tests/layout/test_figure_bridge.py
git commit -m "feat(etl): reconcile detected figures against layout before crop (M3 task 3)"
```

> **Adversarial note:** cắm SAU khi có `detected_regions` và TRƯỚC vòng dựng `panel_lookup`/crop → mọi bước sau chạy trên tập đã lọc (không lệch panel_lookup vì nó dựng lại từ `detected_regions`). Reconcile chỉ **bớt** phần tử, giữ nguyên shape dict → phần metadata không đổi. Wiring thật trên trang thật được QA ở Task 6 (đường QA gọi cùng `reconcile_with_layout`). Kiểm `get_pdf_variant` được import ở scope module (đã có ở cuối file) — nếu không, thêm import cục bộ.

---

### Task 4: Entrypoint dùng `make_image_processor` theo từng cuốn

**Files:**
- Modify: `main.py` (`run_etl_image_only` ~dòng 148–151 & 207; `run_etl` ~dòng 239–243 & 315)
- Test: `tests/etl/test_image_processor_factory.py`

**Interfaces:**
- Consumes: `make_image_processor(filename, status_tracker=None)` (`src.etl.image_processor`).
- Behavior change: mỗi cuốn PDF dùng processor **đúng variant** (CTST/KNTT có subclass), thay vì `ImageProcessor()` base cho tất cả.

- [ ] **Step 1: Write the failing test**

```python
# tests/etl/test_image_processor_factory.py
import src.etl.image_processor as IP


def test_factory_routes_by_filename(monkeypatch):
    # keep construction cheap: stub the heavy collaborators __init__ builds
    monkeypatch.setattr(IP, "ImageCaptioner", lambda *a, **k: object())
    monkeypatch.setattr(IP, "ProcessingStatus", lambda *a, **k: object())

    assert type(IP.make_image_processor("SGK KHTN 6 CTST.pdf")) is IP.CtsstImageProcessor
    assert type(IP.make_image_processor("SGK KHTN 8 KNTT.pdf")) is IP.KnttImageProcessor
    assert type(IP.make_image_processor("SGK KHTN 6 CD.pdf")) is IP.ImageProcessor
```

- [ ] **Step 2: Run test to verify it fails/passes**

Run: `python -m pytest tests/etl/test_image_processor_factory.py -v`
Expected: PASS (factory đã tồn tại) — test này **khoá** hành vi routing mà fix main.py dựa vào. Nếu FAIL (vd `__init__` nạp gì đó ngoài 2 collaborator trên), điều chỉnh monkeypatch cho đúng rồi mới sửa main.py. Tạo `tests/etl/__init__.py` nếu cần.

- [ ] **Step 3: Sửa `main.py`**

Trong `run_etl_image_only`:
- Dòng import (~148): thêm `make_image_processor`:
  `from src.etl import RobustOCRLoader, ImageProcessor, ProcessingStatus, compute_file_hash, make_image_processor`
- Xoá dòng khởi tạo ngoài loop `image_processor = ImageProcessor()` (~151).
- Trong vòng `for pdf_file in ...`, sau khi có `filename`, thêm:
  `image_processor = make_image_processor(filename, status_tracker=status_tracker)`
  (đặt trước chỗ dùng `image_processor.image_extraction_version` / `extract_images_from_pdf`).

Làm y hệt trong `run_etl` (import ~239, khởi tạo ~243 xoá, thêm trong loop sau `filename`).

> Lưu ý: `status_tracker` đã tạo sẵn ngoài loop; truyền vào để processor chia sẻ cùng checkpoint. `make_image_processor` nhận `status_tracker=` (đã có tham số).

- [ ] **Step 4: Run to verify** — `python -m pytest tests/etl/test_image_processor_factory.py -v` → PASS. Smoke: `python -c "import main"` không lỗi.

- [ ] **Step 5: Commit**

```bash
git add main.py tests/etl/
git commit -m "fix(etl): per-variant image processor per book in ETL entrypoints (M3 task 4)"
```

> **Adversarial note:** trước đây batch ETL chạy base `ImageProcessor()` cho MỌI cuốn → CTST/CtsstImageProcessor & KNTT/KnttImageProcessor không kích hoạt (QA tool thì dùng `make_image_processor` → QA lệch production). Sau fix, hai đường khớp. Kiểm `image_processor.image_extraction_version` vẫn đọc được (subclass kế thừa `__init__` base) và `extract_images_from_pdf(pdf_hash=..., ocr_text_per_page=...)` cùng chữ ký. KHÔNG di chuyển việc tạo `status_tracker`/`image_vdb` vào loop (chỉ processor).

---

### Task 5: Fix `đ`-fold trong kênh lexical ảnh

**Files:**
- Modify: `src/rag/image_vectorstore.py` (thêm helper `_fold_accents`; đổi 3 chỗ so khớp tập không-dấu từ `strip_accents(...)` → `_fold_accents(...)`)
- Test: `tests/rag/test_image_dfold.py`

**Interfaces:**
- Consumes: `strip_accents` (`src.rag.query_intent`).
- Produces: module-level `_fold_accents(text: str) -> str` = `strip_accents` + fold `đ/Đ → d/D`. Dùng trong `_tokenize` (so `_STOPWORDS`) và `_query_content_phrases` (so `PHRASE_FILLER_WORDS`, `CLASSIFIER_WORDS`).

- [ ] **Step 1: Write the failing test**

```python
# tests/rag/test_image_dfold.py
import src.rag.image_vectorstore as M
from src.rag.image_vectorstore import ImageVectorDB


def _bare_db():
    return ImageVectorDB.__new__(ImageVectorDB)   # no CLIP / no chroma


def test_fold_accents_folds_d_stroke():
    assert M._fold_accents("được") == "duoc"
    assert M._fold_accents("Đom đóm") == "dom dom"


def test_dstroke_stopword_is_dropped_from_tokens():
    db = _bare_db()
    # "được" is meant to be a stopword ("duoc" is in _STOPWORDS) but strip_accents
    # leaves "đ" so it used to leak in as a content token; folding fixes it.
    toks = db._tokenize("được con trâu")
    bare = [M._fold_accents(t) for t in toks]
    assert "duoc" not in bare


def test_dstroke_filler_dropped_from_phrases():
    db = _bare_db()
    phrases, match_tokens = db._query_content_phrases("cho tôi hình được con trâu")
    joined = " ".join(phrases)
    assert "được" not in joined            # filler folded out
    assert any("trâu" in p for p in phrases)   # real object preserved (accents kept)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/rag/test_image_dfold.py -v`
Expected: FAIL (`_fold_accents` chưa có; và "được" hiện leak vào tokens/phrases).

- [ ] **Step 3: Write minimal implementation**

Trong `src/rag/image_vectorstore.py`, sau block import `from .query_intent import (...)` (dòng ~30–35), thêm:

```python
def _fold_accents(text: str) -> str:
    """strip_accents + fold the Vietnamese stroked D (đ/Đ), which Unicode treats
    as a distinct base letter so strip_accents leaves it (e.g. "được"->"đuoc").
    Membership sets (_STOPWORDS / PHRASE_FILLER_WORDS / CLASSIFIER_WORDS) are
    written đ-free ("duoc"), so compare against the folded form. Mirrors the
    _fold helper in citations.py.
    """
    return strip_accents(text).replace("đ", "d").replace("Đ", "D")
```

Đổi 3 chỗ (đều dạng `strip_accents(token) not in <SET>`):
- `_tokenize` (dòng ~373): `if len(token) > 1 and _fold_accents(token) not in self._STOPWORDS`
- `_query_content_phrases` (dòng ~ token filter): `if len(token) > 1 and _fold_accents(token) not in PHRASE_FILLER_WORDS`
- `_query_content_phrases` (dòng ~ match_tokens): `match_tokens = [t for t in tokens if _fold_accents(t) not in CLASSIFIER_WORDS]`

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/rag/test_image_dfold.py -v` → Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/rag/image_vectorstore.py tests/rag/test_image_dfold.py
git commit -m "fix(rag): fold đ in image lexical stopword/filler matching (M3 task 5)"
```

> **Adversarial note:** CHỈ đổi 3 chỗ **so-khớp-tập-không-dấu**; KHÔNG đụng `strip_accents`/`normalize_accented_text` dùng chung (giữ routing + phrase-accent "trâu"≠"trầu"). Fold order: `strip_accents` trước (bỏ tone mark), rồi `đ→d`. Phrase text dựng bằng `_normalize_accented` (giữ đ ở CẢ query lẫn metadata → khớp đối xứng) nên KHÔNG cần fold ở phần so phrase-in-identity; chỉ tập filler/classifier/stopword cần fold. Test khẳng định "được" bị loại nhưng "trâu" giữ nguyên dấu.

---

### Task 6: QA overlay reconcile + validate CD/CTST/KNTT + docs

**Files:**
- Modify: `src/test/test_image_extraction_full.py` (trong `run_page`: sau `detection = processor.detect_regions_anchor_first(...)`, tính reconcile + vẽ overlay `04_reconciled.png`)
- Modify: `CLAUDE.md` (mục Image ETL: ghi bước reconcile + version), `document/decision_log.html` (thêm D-21…)
- Test: `tests/layout/test_qa_reconcile_smoke.py`

**Interfaces:** none mới — gọi `reconcile_with_layout`/`to_layout_regions` (Task 2). Đây là **cổng QA trực quan** (D-06), acceptance chính của M3.

- [ ] **Step 1: Write the smoke test** (overlay chạy không lỗi trên trang thật; skip nếu vắng corpus)

```python
# tests/layout/test_qa_reconcile_smoke.py
import os
from pathlib import Path

import pytest

PDF = os.path.join("datasources", "SGK KHTN 6 CD.pdf")


@pytest.mark.skipif(not os.path.exists(PDF), reason="corpus not present")
def test_reconcile_overlay_smoke(tmp_path):
    from src.test.test_image_extraction_full import run_page
    from src.etl.image_processor import make_image_processor
    processor = make_image_processor(Path(PDF).name)
    page_dir = tmp_path / "page_008"
    # run_page(processor, pdf_path, page_number, out_dir, keep_old=False);
    # out_dir IS the page dir — it writes overlays directly into it.
    run_page(processor, Path(PDF), 8, page_dir)
    assert (page_dir / "04_reconciled.png").exists()
```

> Chữ ký thật đã kiểm: `run_page(processor: ImageProcessor, pdf_path: Path, page_number: int, out_dir: Path, keep_old=False) -> PageReport`. `out_dir` chính là thư mục trang (ghi thẳng `00_page_snapshot.png` … vào đó). Bên trong: `img_array = np.array(pil_img)` (RGB, 150-DPI) — đúng như production.

- [ ] **Step 2: Run to verify it fails**

Run: `python -m pytest tests/layout/test_qa_reconcile_smoke.py -v`
Expected: FAIL (chưa vẽ `04_reconciled.png`) hoặc SKIP nếu vắng corpus.

- [ ] **Step 3: Thêm overlay reconcile vào `run_page`**

Trong `run_page` (dòng 226–). Sau khối `_save_final_overlay(pil_img, out_dir / "03_final_regions.png", regions, font)` (~dòng 280–281), thêm overlay reconcile:

```python
    from src.etl.layout.figure_bridge import reconcile_with_layout
    from src.etl.image_processor import get_pdf_variant
    kept = reconcile_with_layout(regions, img_array, get_pdf_variant(pdf_path.name))
    kept_bboxes = {tuple(r["bbox"]) for r in kept}
    overlay = pil_img.convert("RGB").copy()
    draw = ImageDraw.Draw(overlay)
    for r in regions:                                  # `regions` = detection["regions"]
        bbox = tuple(r["bbox"])
        colour = (0, 170, 0) if bbox in kept_bboxes else (220, 0, 0)  # kept green / dropped red
        _draw_box(draw, bbox, colour, str(r.get("image_type", "")), font=font)
    overlay.save(out_dir / "04_reconciled.png")
```

(Biến có sẵn trong `run_page`: `regions`, `img_array` (RGB 150-DPI), `pil_img`, `out_dir` (là page dir), `font`, `_draw_box`, `ImageDraw`. Chữ ký `_draw_box(draw, bbox, colour, label, width=3, font=None)`.)

- [ ] **Step 4: Run to verify it passes** — `python -m pytest tests/layout/test_qa_reconcile_smoke.py -v` → PASS (có corpus) / SKIP (không).

- [ ] **Step 5: Chạy QA TRỰC QUAN thủ công (acceptance gate)** — CD trước, rồi CTST/KNTT (~5 trang/variant, ca khó):

```bash
python -m src.test.test_image_extraction_full --pdf "datasources/SGK KHTN 6 CD.pdf"   --pages 6,8,13,131
python -m src.test.test_image_extraction_full --pdf "datasources/SGK KHTN 8 CD.pdf"   --pages 50   # CD8 radial + 97-DPI (đổi số trang theo Hình 8.3/8.4 thực tế)
python -m src.test.test_image_extraction_full --pdf "datasources/SGK KHTN 7 CTST.pdf" --pages 64,135,58,29
python -m src.test.test_image_extraction_full --pdf "datasources/SGK KHTN 6 KNTT.pdf" --pages 13,69,109,131
```

Mở `scripts/_out_test_etl_full/page_<NNN>/04_reconciled.png`. **Pass khi:**
- CD8 radial (Hình 8.3/8.4) **KHÔNG bị drop** (xanh, không đỏ). ← rủi ro #1.
- Sidebar/info-box bị box nhầm-thành-figure **bị drop** (đỏ) đúng chỗ.
- `textbook_info_box`/`activity_box`/`tool_group` **giữ nguyên** (xanh).
- So `03_final_regions.png` (trước) vs `04_reconciled.png` (sau): chỉ **mất figure dương-tính-giả**, KHÔNG mất figure thật.
Nếu radial bị drop nhầm → nâng `FIGURE_IN_BOX_DROP_RATIO` hoặc thêm guard "figure có anchor caption thì miễn-drop" (thêm ở Task 2, không đổi chữ ký). Ghi kết quả QA + số trang thực tế vào decision log.

- [ ] **Step 6: Cập nhật tài liệu + decision log**
  - `CLAUDE.md` (mục "Image ETL"): thêm 1–2 câu — sau `detect_regions_anchor_first`, một bước `reconcile_with_layout` (layout bridge) drop figure dương-tính-giả nằm trong colour box; bump `IMAGE_EXTRACTION_VERSION=v16_layout_reconcile`; entrypoint dùng `make_image_processor` theo cuốn.
  - `document/decision_log.html`: thêm entry `D-21` (format `DECISIONS[]` hiện có, KHÔNG phá script) chốt M3 = reuse + layout bridge, drop-only ratio 0.80, per-variant entrypoint fix, đ-fold; kèm kết quả QA (trang đã kiểm, ngưỡng chốt).

- [ ] **Step 7: Focused suite + commit**

Run: `python -m pytest tests/layout/ tests/rag/test_image_dfold.py tests/etl/ -v` → Expected: PASS (smoke SKIP nếu vắng corpus).

```bash
git add src/test/test_image_extraction_full.py tests/layout/test_qa_reconcile_smoke.py CLAUDE.md document/decision_log.html
git commit -m "test(etl): reconcile QA overlay + M3 docs/decision log (M3 task 6)"
```

> **Adversarial note:** QA tool render 150-DPI y như production → overlay phản ánh đúng hành vi thật. Vẽ TẤT CẢ region detector, tô đỏ cái bị drop → nhìn ngay figure thật có bị mất oan không. Đây là gate mắt (D-06), không thay bằng unit test. Cập nhật auto-memory ([[image_etl_variant_refactor]], [[rag_redesign_2026_08]]) làm ở bước đóng milestone.

---

## Self-Review

**Spec coverage:**
- §4.1 reconcile_with_layout (drop-only, containment, RGB→BGR, same space) → Task 2. §4.2 to_layout_regions → Task 2/3. §4.3 cắm vào extract_images_from_pdf (local import) → Task 3. §5 entrypoint make_image_processor + version bump → Task 1 (version) + Task 4 (entrypoint). §6 đ-fold → Task 5. §8 QA gate CD-first ~5 trang/variant + regression diff → Task 6. Config §7/§10 → Task 1. Rủi ro radial (#1) → Task 6 acceptance + guard dự phòng.
- **Chỉ-figure-type drop** (không drop info/activity/tool) — bổ sung sau khi kiểm nhãn detector thật; phủ bởi `_FIGURE_TYPES` + `test_never_drops_info_or_activity_box` (Task 2).

**Placeholder scan:** mọi step có code thật. Task 6 smoke test + overlay ghi rõ "đọc chữ ký `run_page` thật trước khi viết" (API-dependent) — không phải placeholder mà là chỉ dẫn khớp API hiện có; QA thủ công có lệnh + tiêu chí pass cụ thể.

**Type consistency:** `reconcile_with_layout(regions, image_rgb, variant) -> list[dict]` dùng nhất quán Task 2/3/6; `to_layout_regions(regions) -> list[Region]` Task 2/3/6; `_containment(fig,box)->float` Task 2; `_fold_accents(str)->str` Task 5; `make_image_processor(filename, status_tracker=)` Task 4. `_FIGURE_TYPES` set dùng ở cả reconcile & to_layout_regions.

**Known follow-ups (không chặn M3):** ngưỡng ratio chốt sau QA CD; guard "caption-anchored miễn-drop" chỉ thêm nếu QA radial cần; `close_kernel` theo-DPI chỉ khi QA 150-DPI lệch; chạy ETL ảnh 12 cuốn trên Colab (sau M3, cùng đợt bge-m3 D-18) — ngoài phạm vi code M3.
